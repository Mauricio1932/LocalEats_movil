import 'package:flutter/material.dart';

class MapsScren extends StatelessWidget {
  const MapsScren({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Ubicación"),
        centerTitle: true,
      ),
    );
  }
}
